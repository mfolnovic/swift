<?php

define( "DB_HOST", "localhost" );
define( "DB_USERNAME", "" );
define( "DB_PASSWORD", "" );
define( "DB_DATABASE", "" );
define( "DB_DRIVER", "mysql" );

define( "CACHE", "file" );
define( "CACHE_PATH", TMP_DIR . "app.cache" );

define( "BENCHMARK", false );
define( "LOG", false );

// examples, you can remove this
$router -> addRoute( "%controller%/(%action%/(%id%/))" );

?>
